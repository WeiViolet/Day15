#include<stdio.h>
int greatest(int a,int b,int c);
main()
{
	int a,b,c,g;
	scanf("%d%d%d",&a,&b,&c);
	g=greatest(a,b,c);
	printf("greatest=%d",g);
}
int greatest(int a,int b,int c)
{
	if((a>b)&&(a>c))
	return a;
	else if((b>a)&&(b>c))
	return b;
	else if((c>a)&&(c>b))
	return c;
}
