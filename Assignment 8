#include<stdio.h>
main()
{
	int n,m,i=0,x=0,y=0;
	scanf("%d",&n);
	while(i<n)
	{
		scanf("%d",&m);
		if(m>x)
		{
			y=x;
			x=m;
		}
		else
		y=m;
		i++;
	}
	printf("second greatest nummber is %d",y);
}
