#include<stdio.h>
main()
{
	int i,n,y,evensum=0,oddsum=0;
	scanf("%d%d",&n,&y);
	for(i=1;i<=n;i++)
	{
		if(i%y==0)
		{
			printf("%d \n",i);
				if(i%2==0)
				{
					evensum+=i;
				}
				else
				{
					oddsum+=i;
				}
		}
	}
	printf("even sum=%d\nodd sum=%d",evensum,oddsum);
}
